#summary what has to be done for the next "release"

= Introduction =

This that need to be done in the near future.


= Details =
 * query the available preview sizes and preview formats and use one that fits our needs 
   * this function is not available on 1.5/1.6 avoid a VerifyError(Test in the Emulator
 * fix AndAR on milestone 2.1
 * fix unusual preview sizes, like not 240x160 .. does not work currently
 * error handling
   * are all exceptions forwarded? or some suppressed?
   * how may a class extending AndARActivity inform the user about those exceptions